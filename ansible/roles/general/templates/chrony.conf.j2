{% for ntp in ntp_sources.nimt %}
server {{ ntp }} iburst minpoll 12 maxpoll 16
{% endfor %}

{% for pool in ntp_sources.ntp_pool %}
pool {{ pool }} iburst
{% endfor %}

driftfile /var/lib/chrony/drift
makestep 0.1 3

rtconutc
rtcsync
