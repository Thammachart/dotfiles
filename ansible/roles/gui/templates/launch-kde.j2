#!/bin/sh

export XDG_SESSION_DESKTOP=plasma
export XDG_CURRENT_DESKTOP=plasma

source {{ wayland_enablement_path }}
source {{ load_systemd_envs_path }}

exec systemd-cat --identifier=kde dbus-run-session startplasma-wayland