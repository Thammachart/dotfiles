# Go
{{ if not (contains "Work" .chezmoi.hostname) -}}
GOPATH=/mnt/data/.local/.go
{{ else }}
GOPATH={{ .chezmoi.homeDir }}/.local/go
{{ end -}}

# Java
{{ if (eq .chezmoi.osRelease.id "arch") -}}
JAVA_HOME=/usr/lib/jvm/default
{{ end -}}

{{ if not (contains "Work" .chezmoi.hostname) -}}
# Pulumi
PULUMI_HOME=/mnt/data/.local/.pulumi
{{ end -}}